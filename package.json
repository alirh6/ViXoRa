{
  "name": "vixora",
  "private": true,
  "version": "2.4.0",
  "type": "module",
  "description": "ViXoRa — Enterprise Management Platform (functional, dependency-free, localStorage-first)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "lint": "node --check src/app.js",
    "server": "json-server --watch src/db/db.json --port 3001"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vite": "^8.2.2"
  }
}
